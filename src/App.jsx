function App() {
  return (
    <div>
      <h1>SecureVault</h1>
      <p>Temporary File Storage System</p>
    </div>
  )
}

export default App
```

**7.3** Open `src/App.css` → select all → delete everything (leave the file empty)

**7.4** Open `src/index.css` → select all → delete everything (leave the file empty)

**7.5** Save all files (**Ctrl + S**)

**7.6** Check browser → should now show just:
```
SecureVault
Temporary File Storage System